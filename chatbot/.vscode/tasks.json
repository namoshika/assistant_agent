{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Deploy databricks",
            "type": "shell",
            "command": "databricks bundle validate && databricks bundle deploy -t dev && databricks bundle run webui -t dev",
            "problemMatcher": []
        }
    ]
}