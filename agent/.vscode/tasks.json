{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Launch DevServer",
            "type": "shell",
            "command": "uv run fastapi dev --host agent --reload",
            "problemMatcher": [
                "$python"
            ],
            "isBackground": true,
            "group": "build"
        }
    ]
}