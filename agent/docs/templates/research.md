# Research: {機能名}

<!-- requirements.md と同じ機能名。
     役割: 調べて分かった客観的事実を集積する（API仕様・制約・検証結果・web資料URL・関連ADR）。
     「この方法を採用する」といった決定は書かない → design.md へ（理由の記述はここでもよい）。
     判定基準: 実装コードなしで成立する記述か。対話中に得た調査結果も都度ここへ反映する。 -->

## 使用パッケージ

<!-- 調査で候補に挙がった／使うことにしたパッケージとバージョン。不採用分も調査済みなら記載してよい。 -->

> 例:
> | パッケージ | バージョン | 用途 |
> |---|---|---|
> | `duckdb` | 1.3.2 | DuckDB 本体 |

## {調査トピック}

<!-- 調査対象ごとに見出しを立てる。ソース確認ならファイルパス・行番号、web資料なら URL を添える。 -->

> 例: `DuckDBVectorStore.add()` はメタデータに `str/int/float/None` のみ許容する
> （実地検証: `list` を渡すと `ValueError: Value for metadata ... must be one of (str, int, float, None)`）。

## 関連 ADR

<!-- docs/adr/ 配下で関連する ADR を1文で。なければ「該当 ADR なし」。 -->
